name: Apply Verified Final Retirement Patch

on:
  push:
    branches:
      - fix/final-external-channel-data-retirement

permissions:
  contents: write
  statuses: write

concurrency:
  group: apply-final-retirement-patch
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-24.04
    if: ${{ !contains(github.event.head_commit.message, 'complete retired persistence convergence') }}
    steps:
      - name: Publish exact run identity
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          REPOSITORY: ${{ github.repository }}
        run: |
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${REPOSITORY}/statuses/${SHA}" \
            --data "{\"state\":\"pending\",\"context\":\"apply-final-retirement-patch\",\"description\":\"run_id=${RUN_ID}\",\"target_url\":\"https://github.com/${REPOSITORY}/actions/runs/${RUN_ID}\"}"

      - name: Checkout exact branch Head
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: fix/final-external-channel-data-retirement
          fetch-depth: 0
          persist-credentials: true

      - name: Reconstruct and verify exact patch
        shell: bash
        run: |
          set -uo pipefail
          mkdir -p /tmp/retirement-diagnostics
          mapfile -t parts < <(find .github -maxdepth 1 -type f -name 'final-retirement.patch.gz.b64.part*' | sort)
          printf '%s\n' "${parts[@]}" > /tmp/retirement-diagnostics/parts.txt
          wc -c "${parts[@]}" > /tmp/retirement-diagnostics/part-sizes.txt
          test "${#parts[@]}" -gt 0 || { echo no_parts > /tmp/retirement-diagnostics/error.txt; exit 1; }
          cat "${parts[@]}" > /tmp/final-retirement.patch.gz.b64
          if ! base64 --decode /tmp/final-retirement.patch.gz.b64 > /tmp/final-retirement.patch.gz 2>/tmp/retirement-diagnostics/base64-error.txt; then exit 1; fi
          if ! gzip -dc /tmp/final-retirement.patch.gz > /tmp/final-retirement.patch 2>/tmp/retirement-diagnostics/gzip-error.txt; then exit 1; fi
          sha256sum /tmp/final-retirement.patch > /tmp/retirement-diagnostics/actual-sha256.txt
          if ! echo '3c573b2b25429ac63fba8bdaea4d539d698b2b3447cc6d87703879ebe6d55bd6  /tmp/final-retirement.patch' | sha256sum --check --strict > /tmp/retirement-diagnostics/sha-check.txt 2>&1; then exit 1; fi
          if ! git apply --check /tmp/final-retirement.patch > /tmp/retirement-diagnostics/apply-check.txt 2>&1; then exit 1; fi

      - name: Upload reconstruction diagnostics
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: retirement-apply-diagnostics-${{ github.sha }}
          path: /tmp/retirement-diagnostics
          if-no-files-found: error
          retention-days: 1

      - name: Apply atomically and remove delivery tooling
        shell: bash
        run: |
          set -euo pipefail
          git apply --index /tmp/final-retirement.patch
          git rm -f --ignore-unmatch vendor/chatwoot
          rm -f .github/final-retirement.patch.gz.b64.part*
          rm -f .github/workflows/_apply-final-retirement.yml
          git add -A
          git diff --cached --check
          git config user.name 'nexus-governance-bot'
          git config user.email 'nexus-governance-bot@users.noreply.github.com'
          git commit -m 'fix: complete retired persistence convergence'
          git push origin HEAD:fix/final-external-channel-data-retirement

      - name: Publish final application status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          REPOSITORY: ${{ github.repository }}
          JOB_STATUS: ${{ job.status }}
        run: |
          state=failure
          if [ "${JOB_STATUS}" = success ]; then state=success; fi
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${REPOSITORY}/statuses/${SHA}" \
            --data "{\"state\":\"${state}\",\"context\":\"apply-final-retirement-patch\",\"description\":\"run_id=${RUN_ID}; job=${JOB_STATUS}\",\"target_url\":\"https://github.com/${REPOSITORY}/actions/runs/${RUN_ID}\"}"
