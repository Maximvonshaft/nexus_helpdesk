# Round20B legacy frontend production hardening

> Historical delivery evidence. The legacy frontend referenced here is retired and must not be restored.

Historical changes included removal of API-address exposure, read-only bulletin access for operators, role-scoped management surfaces, and differentiated dashboard metrics.

Current frontend authority is `webapp/`; current architecture and verification contracts are defined by the root `README.md`, `config/architecture/`, and `scripts/verify_repository.py`.
