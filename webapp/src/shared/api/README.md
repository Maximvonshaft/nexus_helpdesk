# shared/api

Target home for shared HTTP client, API errors, auth header handling, and API compatibility wrappers.

Current active API client remains `webapp/src/lib/api.ts` until a reviewed migration step moves behavior safely.
