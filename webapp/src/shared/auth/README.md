# shared/auth

Target home for shared auth/session utilities and authenticated UI guards.

Current active auth helpers remain in their existing files until a reviewed behavior-preserving migration step.
