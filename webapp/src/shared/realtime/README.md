# shared/realtime

Target home for event stream client, event types, reconnect/dedupe logic, and query-cache event handlers.

No realtime runtime is active in this foundation branch. Existing polling behavior remains unchanged.
