# shared/ui

Target home for reusable primitive and business UI components.

Current active UI components remain in their existing locations until the design-system phase migrates them with visual smoke evidence.
